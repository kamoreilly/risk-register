# Dashboard Enhancement Plan

## Status: APPROVED FOR IMPLEMENTATION

## Current State

The dashboard (`frontend/apps/web/src/routes/app/index.tsx`) has:
- Risk Severity Distribution pie chart
- Risks by Category horizontal bar chart  
- Upcoming Reviews list
- Risk Distribution Details with progress bars
- Basic loading skeleton and error state

## Reference: Analytics Page

The analytics page (`frontend/apps/web/src/routes/app/analytics.tsx`) demonstrates a more polished layout with:
- Summary stat cards (4 cards: Total Active Risks, Critical Exposure, High Likelihood, Mitigation Rate)
- Section headers with icons
- Two-column chart layouts
- Better visual hierarchy

## Approved Enhancements

### 1. Add Summary Stat Cards (Top Row)
4 stat cards showing:
- **Total Active Risks** - from `total_risks`
- **Critical Risks** - from `by_severity.critical`
- **Overdue Reviews** - from `overdue_reviews`
- **Upcoming Reviews** - count from reviews data

### 2. Reorganize Layout Structure
```
Row 1: 4 Stat Cards (full width)
Row 2: Severity Pie Chart | Category Bar Chart (2:1 ratio)
Row 3: Upcoming Reviews Card | Risk Distribution Progress (1:1 ratio)
```

### 3. Improve Visual Polish
- Add section header with icon for upcoming reviews
- Use existing `ProgressReport` component for distribution
- Add subtle hover effects to cards
- Improve empty state presentation

## Components to Reuse
- `StatCard` - already exists
- `ProgressReport` - already exists
- `Badge` - already exists
- `Card` family - already exists

## Implementation Steps

1. Add summary stat cards row at top of dashboard
2. Restructure chart cards to use proper 2:1 grid
3. Add section header with icon to upcoming reviews
4. Replace inline SeverityProgress with `ProgressReport` component
5. Improve loading and error states if needed

## Not Doing (to avoid over-engineering)
- No new chart types
- No complex interactions
- No new API endpoints
- No state management changes
- Keep existing data hooks intact